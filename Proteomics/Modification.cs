using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace RCPA.Proteomics
{/*
  public class Modification : List<KeyValuePair<char, double>>
  {
    public void Parse(string value)
    {
      Clear();
      if (value.Trim().Length > 0)
      {
        string[] parts = value.Split(new[] { ',', ' ' });
        for (int i = 0; i < parts.Length; i++)
        {
          Match m = this.g.Match(parts[i]);
          Debug.Assert(m.Success, MyConvert.Format("Fail to parse modification {0} of {1}", parts[i], value));
          Add(new KeyValuePair<string, string>(m.Groups[2].Value, m.Groups[1].Value));
          this._modificationMap[i + 1] = MyConvert.Format("{0} ({1})", m.Groups[1], m.Groups[2]);
        }
      }

    }
  }
  */ 
}
